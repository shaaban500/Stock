@using Stock.Domain.ViewModels.Stores;
@model List<StoreViewModel>
@{
    ViewData["Title"] = "Stores";
}


<div class="d-flex justify-content-center mb-5 w-100">
    <div class="d-flex justify-content-between align-items-center w-75">
        <h3>
            <i class="fa-solid fa-shop"></i>
            Stores
        </h3>
        <div class="d-flex flex-column align-items-center justify-content-center">
            <a onclick="showInPopup('@Url.Action("AddOrEdit","Stores",null,Context.Request.Scheme)','New Store')" class="btn btn-dark">
                <i class="fas fa-add me-2"></i>
                add store
            </a>
        </div>
    </div>
</div>



<div class="d-flex justify-content-center">
    <table class="table text-center w-75">
        <thead>
            <tr>
                <th>
                    Id
                </th>
                <th>
                    Name
                </th>
                <th>
                    Actions
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Id)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        <div class="d-flex justify-content-center gap-3">
                            <a onclick="showInPopup('@Url.Action("AddOrEdit","Stores",new {id=item.Id},Context.Request.Scheme)','Update Store')"
                               class="text-primary">
                                <i class="fas fa-pencil-alt"></i>
                            </a>
                            <a asp-asp-controller="Stores" asp-action="Delete" asp-route-id="@item.Id" class="text-danger">
                                <i class="fas fa-trash-alt"></i>
                            </a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>